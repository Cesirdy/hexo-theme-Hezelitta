<nav id="nav" class="main-nav">
	<% if (!is_home()){ %>
		<a href="<%- config.root %>"><%= __('home') %></a>
	<% } %>
	<% for (var i in theme.menu){ %>
		<a class="<%= is_current(theme.menu[i]) ? 'cta' : '' %>" href="<%- url_for(theme.menu[i]) %>"><%= i %></a>
	<% } %>
</nav>